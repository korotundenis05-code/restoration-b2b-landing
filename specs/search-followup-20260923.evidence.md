# Search follow-up evidence, 2026-09-23

Scope: revision `d10864e223b743dbd56b4b18815f86403b680eef` against baseline `6360103f17ca5bc8d8b6e1ab185a291b0b81f2a1`.

| ID | Implementation and direct evidence | Verification | Remaining uncertainty |
| --- | --- | --- | --- |
| R1 | No address, hours, review, fixed tariff, or Yandex Business photo was added. The public profile says services are provided in Saint Petersburg; no customer walk-in location was asserted. | Reviewed the complete code diff and public profile `https://yandex.ru/profile/157427855735`. | Yandex Business asks for photos to complete the profile, but the owner declined uploads. |
| R2 | `index.html` removes a quotation process mislabeled as `priceRange`, adds the official profile as business `sameAs`, and displays a footer link; `assets/css/styles.css` underlines footer links. | `test_public_business_profile_identity`; live home-page inspection; production byte match. | `sameAs` is identity markup, not a guarantee of search ranking or AI citation. |
| R3 | `WebSite` and `WebPage` `dateModified` and the home-page sitemap `lastmod` reflect the 2026-09-23 release. | `test_homepage_modified_dates_match_release`; site validator. | Search engines may retain older crawls until they revisit the page. |
| R4 | Pushed revision `d10864e` to `main`; GitHub Pages run `35868283491` completed successfully. | 38 unit tests passed with the source-photo root configured; `python3 scripts/validate_site.py` passed 18 canonical pages; `python3 scripts/verify_live.py` passed all 18 pages and 8 supporting files with exact bytes and confirmed YandexBot, Googlebot, Bingbot, and OAI-SearchBot access. `git diff --check` passed. | A first local live-check attempt failed solely because sandbox DNS/network access was disabled; its authorized network rerun passed. |
| R5 | Yandex Webmaster accepted a 2026-09-23 home-page recrawl request (queued). Google Search Console's live test said the URL is available and indexable, then confirmed a priority indexing request. IndexNow returned HTTP 200 for the changed home page. | Observed owner-dashboard confirmation messages and IndexNow response. | These are submission receipts, not proof of fresh indexing or rank. Yandex still lists 11 low-value/low-demand exclusions last crawled 2026-09-14, before the Sep 22 content revision. Google Search Console says `sitemap.xml` could not be processed on Sep 22 despite the public XML being valid and HTTP 200; a text fallback is submitted. Bing previously showed 18 discovered and 2 indexed; Alice AI lacked sufficient data. A sampled ChatGPT local-service answer did not cite the site. |

The text publication appears in the Yandex Business owner interface but is not visible in the public profile's News section as checked on 2026-09-23; no public-publication claim is made. No ranking or AI-platform inclusion target is recorded as achieved.
