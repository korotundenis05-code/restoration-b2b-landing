# Service search coverage, 2026-09-09

## Objective and baseline

Improve truthful service coverage and search/AI retrieval eligibility for restb2b.fun, then publish and verify production. Baseline: 04e37e0. The project is static HTML/CSS/JS on GitHub Pages; 12 canonical pages already exist. No tracked user edits were present. Root .DS_Store, assets/.DS_Store, the ZIP, and UPLOAD_TO_GITHUB are pre-existing untracked artifacts.

## Authorization and confirmed scope

The user explicitly authorized changes and production publication. On 2026-09-09 the user confirmed local chip restoration on acrylic and ceramic bathtubs. Full recoating and cast-iron/steel bath services are not confirmed. Existing ceramic sink, toilet, glaze and B2B batch services remain in scope.

## Requirements

- R1: Add three substantive service pages: ceramic sink restoration, ceramic toilet restoration, and local acrylic/ceramic bath chip repair. Include direct scope, defect assessment, quote inputs, photos from existing real work, limitations, process, price explanation, contacts, visible FAQ and related links.
- R2: Update the homepage and existing ceramic service hub to address broad ceramic/sanitary restoration queries and link directly to the three product services. Reuse existing URLs for overlapping intents; do not generate doorway pages for spelling or city variants.
- R3: Keep HTML content and WebPage/Service/BreadcrumbList/FAQPage JSON-LD aligned. Update factual business service catalog and machine summaries to include the confirmed bath work. Preserve old canonical URLs, verification files, crawl access, email, phone and gallery expansion.
- R4: Add all new URLs to sitemap.xml and both LLM summary files. Change lastmod only for pages actually changed. Record a query-to-page map and actual search observations without claiming exact rankings from an aggregated search tool.
- R5: Improve relevant rendering issues discovered in current UI and keep all content readable at 390 px and 1440 px, without horizontal overflow. Use static HTML and existing images; no new client-side framework or unnecessary scripts.
- R6: Validate HTML on all sitemap pages; parse structured data; verify FAQ parity, unique metadata, links/anchors/assets, sitemap completeness, JS syntax, and changed-file whitespace. Browser-check homepage gallery and navigation plus the new product pages on desktop/mobile. Review the actual diff with an independent reviewer when available.
- R7: Publish reviewed changes to origin/main, verify live payload equality for changed HTML/CSS and discovery files, check crawl headers/robots and submit changed URLs via IndexNow. Refresh the user's primary local project, ZIP and UPLOAD_TO_GITHUB.

## Boundaries and uncertainty

No invented prices, credentials, guarantees, reviews, workshop street address, service methods or on-site visits. No new analytics tracker without a configured account. Do not treat a successful user-agent probe as proof a real crawler visited. No claim that IndexNow guarantees indexing. No guarantee of top-three ranking or citation in every AI product. AI search eligibility and observed references are separate from model training.

## Acceptance and checks

R1-R4: static content review, schema/link validator, query map. R5-R6: HTML validation, JS syntax, browser UI and responsive DOM checks, review. R7: Git remote SHA, production status/content comparison, IndexNow status, ZIP and upload content comparison. Lighthouse is diagnostic, not a ranking measure.

## Completion

All code and deployment requirements pass with direct evidence. Unavailable search-console or AI-account observations are reported as unavailable, never zero. Ranking remains an external objective, not an achieved acceptance claim.
